interface:
  display_name: "Columbia Pages"
  short_description: "Publish shareable pages and files with cpages"
  default_prompt: "Use $columbia-pages to publish this page or file and return its shareable URL."
